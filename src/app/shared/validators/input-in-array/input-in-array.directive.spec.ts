import { InputInArrayDirective } from './input-in-array.directive';

describe('InputInArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new InputInArrayDirective();
    expect(directive).toBeTruthy();
  });
});
